import React from 'react'
import {list} from './UserList.module.css'
import { Card } from '../../UI'

const UserList = () => {
  return (
    <Card className={list}>
      UserList
    </Card>
  )
}

export default UserList